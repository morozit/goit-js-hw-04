// ? 1. Як викликати функцію?
// ()
// ? 2. як викликати метод об'єкта?

// ? метод об'єкта це певна функція. Щоб викликати метод об'єкта  потрібно прописати "об'єкт"."властивість(функція)""(і сам виклик, в дужках передаєються певні ПАРАМЕТРИ )

// ? 3. як викликати об'єкт?
